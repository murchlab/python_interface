# -*- coding: utf-8 -*-
"""
Created on Fri Feb 14 17:41:28 2020

@author: crow104
"""
import numpy as np

#c = [[np.hstack((a1, b1)) for a1, b1 in zip(a_channel, b_channel)] for a_channel, b_channel in zip(a, b)]
#print(c)
#print(c[0][0].shape)
#
#c = np.zeros((4,3,102,8000))
#
#c[:, :, :51, :] = a_np
#c[:, :, 51:, :] = b_np
#
#print(c.shape)
#

num_patterns = 8
num_channels = 4
#np.array([ [None,None,None] for i in range(num_channels) ])
print(channel_array.shape)